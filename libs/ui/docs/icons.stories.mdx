import { Meta } from "@storybook/addon-docs/blocks";
import {
  Box,
  Wrap,
  WrapItem,
  Text,
  Stack,
  Heading,
} from '@chakra-ui/react'
import { is, has } from "ramda"
import * as icons from "@chakra-ui/icons"
import * as boxIcons from "react-icons/bi"

<Meta
  title="Docs / Icons"
  id="icons"
/>

# Icons

<Wrap spacing={4} my={10}>
  {Object.entries(icons).map(([iconName, Comp]) => {
    if (iconName === "Icon" || !is(Object, Comp) || !has("render", Comp)) return null
    return (
      <WrapItem key={iconName}>
        <Box 
          display="flex"
          flexDirection="column"
          justifyContent="center"
          alignItems="center"
          bgColor="gray.100"
          rounded={5}
          minWidth="150px"
          minHeight="100px"
          p={4}
        >
          <Comp fontSize="24px" mb={4}/>
          <Text as="strong" fontSize="12px" maxW="100px" isTruncated>{iconName}</Text>
        </Box>
      </WrapItem>
    )
  })}
</Wrap>

# Box Icons

<Wrap spacing={4} my={10}>
  {Object.entries(boxIcons).map(([iconName, Comp]) => {
    const { Icon } = icons
    return (
      <WrapItem key={iconName}>
        <Box 
          display="flex"
          flexDirection="column"
          justifyContent="center"
          alignItems="center"
          bgColor="gray.100"
          rounded={5}
          minWidth="150px"
          minHeight="100px"
          p={4}
        >
          <Icon as={Comp} fontSize="24px" mb={3} />
          <Text as="strong" fontSize="12px" maxW="100px" isTruncated>{iconName}</Text>
        </Box>
      </WrapItem>
    )
  })}
</Wrap>