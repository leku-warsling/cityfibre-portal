import { Meta } from '@storybook/addon-docs/blocks';
import { Box, Wrap, WrapItem, Text } from '@chakra-ui/react';
import { withPixelRef, toKeyValueCollection, remToPx } from './util';
import Table from './components/Table';
import theme from '../src/theme';

<Meta title="Docs / Theme / Sizing" id="sizing" />

# Sizing

## Spacing - Padding | Margin

<Table
  columns={[
    {
      key: 'key',
      title: 'Name',
    },
    {
      key: 'value',
      title: 'Value',
    },
    {
      key: 'value',
      title: 'Example',
      render: (value) => <Box bgColor="#FED7D7" w={value} h="25px" />,
    },
  ]}
  data={toKeyValueCollection(theme.space).sort((a, b) => parseFloat(a.value) - parseFloat(b.value))}
/>

<Wrap spacing={4} my={10}>
  {Object.entries(theme.space).sort(([,a], [,b]) => parseFloat(a) - parseFloat(b)).map(([space, value], idx) => (
    <WrapItem key={idx}>
      <Box 
        p={space}
        bgColor="#FED7D7"
        fontSize="lg"
        border="1px dashed"
        borderColor="#d10505"
      >
        <Box
          display="flex"
          flexDirection="column"
          justifyContent="center"
          alignItems="center"
          w="100px"
          h="100px"
          bgColor="white"
          border="1px dashed"
          borderColor="#d10505"
        >
          <Text fontSize="lg" fontWeight={600}>
            {space}
          </Text>
          <Text fontSize="sm">{value}</Text>
          <Text fontSize="sm">{remToPx(value)}</Text>
        </Box>
      </Box>
    </WrapItem>
  ))}
</Wrap>

## Breakpoints
## Containers